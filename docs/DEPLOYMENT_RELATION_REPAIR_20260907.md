# 数据源关系修复与 TypeScript 清理部署记录

日期：2026-09-07。目标服务器：39.107.117.246。用户已指定该服务器继续实施，并选择先对现有已确认关系做回归。未创建 Git commit。

最新交付为第三阶段最终补丁：`ontology-query-platform-ontoquery:relation-scope-20260907T1457Z`，镜像 `sha256:2c5ad5e66a0b1032097942ce479adb182fd5e6690fa07f22aab2dcf06bc047fb`。完整回归和生产关系识别已完成，当前构建有 104 条业务关系待确认；已发布 v11 未变。下文按阶段保留历史结果。

## 部署结果

- 第二次发布镜像（含采样超时修复，第三阶段发布见后文）：`sha256:e38576a63aad93fedd8d030f1023c2d9ffb8ffa2e6b09438750292102f72eede`；首次发布镜像为 `sha256:e8500fe24be1879fc2c211e3e71d5ad17cce52c5acd428d315748c9b0eb9a871`。
- 发布目录：`/data/ontoquery-releases/relation-repair-20260907T085355Z`。
- 回退镜像：`ontology-query-platform-ontoquery:relation-repair-20260907T085355Z-backup`，指向原镜像 `sha256:0896420c978eac9dcfc60efc40e84ad25c0e0725bbd1718768d815bc6d1ce72d`。
- 保留原 `.env.production`、`.env.local`（若存在）、`compose.override.yaml` 和 runtime 挂载。配置文件哈希在切换前后保持一致。
- 停止服务后备份一致性 SQLite、完整数据与知识目录，再切换已验证的新镜像；部署脚本包含失败回退。
- 外部网页 `http://39.107.117.246/` 返回 200；容器健康，API ready 返回 200，未认证 bootstrap 返回 401。

## 构建与迁移证据

源码按当前工作区打包，不依赖尚未提交的 Git 状态。输入包包含 250 个文件，SHA-256 为 `71dbd1d8e0e29f31d13a6ca34843e7b772253f37b45e87fbfa6b268648e3c9c0`。清单 SHA-256 为 `3c3a2997185ad8365f6768216c06c6dd408426cae4779e63ff8db795349c68c6`。

依赖清单及 package-lock 与运行镜像一致，复用其 Linux 依赖后，在无外网、未挂载生产数据的候选镜像中运行 `npm run check`：TypeScript 0 错误、ESLint 与构建通过、645 项测试通过、页面渲染 1 项通过。

生产数据库副本的迁移预演逐表比较 32 张表：

- 1,412 条历史关系保留原 ID 和原始业务字段；本库没有需要停用的旧 foreign_key 记录。
- 纠正 17 个不能由旧目录证明的唯一键标记；新增完整列组和证据字段。
- SQLite quick_check 为 ok，foreign_key_check 无错误。
- 当前已发布 Schema 校验通过。启动后真实数据源、关系、字段、知识、版本、设置及选表指纹保持符合预期。

线上核验确认 248 个文件与源码输入逐一匹配。其余两项为明确的构建处理：`.env.example` 按 `.dockerignore` 规则不进入镜像；Docker 为外部构建文件向 `.dockerignore` 追加一个随机文件名排除项，去除该自动追加项后原内容哈希一致。

11 类生产业务表按迁移预期逐项比较通过。当前生效版本仍为 id 12 / v11，包含 26 个对象、36 条业务关系。保留 auto_draft / 85、Claude required / glm-5.2-fast-preview、SQL 预算 5，列画像关闭。

Nginx 实际返回 `/_next/static/chunks/platform-app-juyJHVCK.js`，与容器资源内容一致，SHA-256：`9f6c2ec60ffc371cd9dde8ad69feeb0c8d720f76a9366b405a40cc375eee0524`。

## 历史关系回归准备与当前边界

已从发布前目录导出当前 26 张选中表内、去重后的 90 条已确认关系，使用 `complete=false`。该清单是历史回归参考，包含既往模型或文档辅助确认结果，不是独立业务真值；已验证知识也可能包含这些关系，不能据此标定真实精确率或自动确认阈值。服务器没有配置 Gold SQL 评测集。

在独立容器中使用元数据只读副本、只读业务查询和现有模型配置执行评测。模型服务为 `https://dashscope.aliyuncs.com`，模型 `glm-5.2-fast-preview`，列画像保持关闭；模型输入包含结构、注释、关系匹配统计及已验证业务知识。

用户已明确回复“允许本次真实模型回归”，此前自动审批审查要求的数据出站授权已满足。已启动容器 `ontoquery-relations-regression-20260907T085355Z`。启动校验强制核对服务域名、模型、数据源、26 张选表与 90 条参考关系、列画像关闭和 85 分门槛；任一不符即停止。容器不挂载生产 runtime，元数据副本只读挂载，评测工作库在临时目录中。

真实 MySQL 与模型回归随后已完成。生产目录重探、本体重建或发布新版本均未执行，阈值未调整；结果见下节。

## 采样超时修复与真实回归结果（2026-09-07）

第一轮回归暴露出一个运行时问题：应用侧 AbortController 销毁客户端连接后，MySQL 5.7 上已开始执行的相关子查询仍可能继续运行，造成只读账号的长查询堆积。修复在每个关系分层查询和匹配查询的第一个 `SELECT` 加入唯一的 `MAX_EXECUTION_TIME` 服务端提示，并把服务端超时记录为 `timeout` 证据，不把它当成零匹配。新增回归覆盖每条采样语句的服务端截止时间和超时证据分类。

服务端提示的位置和对 UNION/子查询的作用范围依据 [MySQL 5.7 官方文档](https://dev.mysql.com/doc/refman/5.7/en/optimizer-hints.html)。本次只修改关系采样模块；尚不能据此声称应用所有 SQL 入口都有服务端超时保护。

最终本地验证：TypeScript 0 错误、ESLint 与构建通过；646 / 646 项测试通过，页面渲染 1 / 1 通过。受限沙箱中两个 Claude 集成用例因本机监听限制失败，允许本机监听后全量通过，没有修改这些既有用例或降低断言。

修复镜像为 `sha256:e38576a63aad93fedd8d030f1023c2d9ffb8ffa2e6b09438750292102f72eede`，原线上镜像保留为 `ontology-query-platform-ontoquery:relation-sampling-timeout-20260907T1010Z-backup`。热修复切换后 API healthy，源码清单 248 个文件精确匹配（250 个输入文件中的 `.env.example` 与 Docker 自动追加排除项仍按先前规则处理），已发布版本、目录关系和设置未变化。切换后运行清理检查，匹配本次选表的遗留采样查询为 0。

收尾时将热修复模块及两份更新后的测试同步回服务器构建源码，先校验并备份旧文件，再核对 3 个文件的哈希，避免后续重新构建丢失修复。源码与备份保存在发布目录的 `timeout-hotfix/`，当前镜像与源码同步结果记录在 `hotfix-deployment-result.json`。回归结束后的生产核验再次通过：11 类业务表符合迁移预期，SQLite quick_check 为 ok，API ready 正常；`production-verification.json` 已更新，原 `deployment-result.json` 保留首次发布的历史记录。

第二轮在隔离容器中完成真实回归：

- MySQL `5.7.44-log`；26 张选表、679 个字段；没有显式外键，26 张表均进入分析（A 级 11、B 级 15）。
- 历史参考 90 条已确认关系，`complete=false`，不是独立 Gold 集。候选 279 条，模型调用 27 次，报告 token 243,959，耗时 675,556 ms（约 11.3 分钟），关系采样查询 904 次。
- 进入候选的历史关系为 83 / 90，候选召回 `92.22%`。评测阈值按关系模型默认 `0.55`；达到模型判断阈值的历史正例为 65 / 90，已标注集合内精度 `100%`、标注召回 `72.22%`。仍有 51 条正向预测落在未标注范围，故完整精度为 `null`，不能把 `100%` 当成真实精度。
- 方向准确率 `81.54%`，基数准确率 `67.21%`（只在有对应标注的命中上计算）。阈值扫描中，`0.85` 的标注召回为 `53.33%`。这里扫描的是关系模型置信度，与本体自动确认的综合评分 85 分属于不同指标；生产自动确认门槛没有因本次回归改动。
- 主动提案 10 / 10 批完成，但部分批次超出单批提案预算，状态为 `partial`；首轮证据不足关系 100 条，其中 40 条按预算完成补采样，60 条保留未补采样。因此容器退出码为 2（部分完成），不是崩溃或生产写入失败。
- 第二轮结束后遗留采样查询为 0。未确认、未发布新关系，也没有重建或切换线上本体；线上仍为 id 12 / v11、26 个对象、36 条业务关系。

这些数字是“历史已确认关系回归”，受既有确认偏差、已验证知识复用和未标注关系影响，只能用于当前流程的回归对照。要计算真实全量精确率、召回率和校准 85 门槛，仍需独立反例与完整业务真值或 Gold SQL 评测集。

完整报告保存在服务器发布目录的 `regression/report.json`，同目录 `report.json.predictions.json` 可用于离线重算，不必再次调用模型。报告中的 27 次调用与 token 用量只覆盖第二轮，不包含第一轮中止前已经发生的模型调用。没有提供模型价格，未推算费用。

## 回退资料

发布目录中保留 `platform-before-cutover.sqlite`、`runtime-before.tgz`、`source-and-config-before.tgz`、`source-manifest.json`、构建与切换日志，以及 `preflight/migration-report.json`、`production-verification.json`。数据库和环境配置备份仅存于服务器的私有发布目录，不随本文或源码分发。

若需回退，应先停止容器、保留切换后的数据副本，再恢复一致性数据与知识备份、原服务器配置和回退镜像。切换后如已产生新业务写入，应先评估并保留这些新增记录，不直接用旧数据库覆盖。

## 第三阶段：持久化检查点已上线（2026-09-07）

检查点首次发布镜像为 `sha256:247b33d0056967004e06cdd1ed63cdbae72c074e1f05d43e0f2910eea8f7ad09`，标签 `ontology-query-platform-ontoquery:relation-checkpoint-20260907T1343Z`。本轮发布目录为 `/data/ontoquery-releases/relation-checkpoint-20260907T1343Z`；上一版超时修复镜像保留为同标签加 `-backup`。生产验收后补充的最终修复见下文。

已接入关系提案、逐条采样、逐批判断、补采样复核的共同检查点，生产后台任务和隔离评测均使用该入口。模型超额合法提案排队保存；漏答复核只补问。恢复核对结构、选表、模型、知识与配置，保存失败不会当成模型失败再调用。页面有“继续关系识别”；本体构建在关系分析未完成时保留可重试事项。完整参数及边界见 [评测说明](SOURCE_RELATION_EVALUATION.md)。

本轮新增 13 项测试。服务器在锁定依赖、无外网和无生产数据挂载的镜像构建中通过 `npm run check`：TypeScript 0 错误、ESLint 和构建通过、**659 / 659 项测试通过**、页面渲染 **1 / 1** 通过。迁移副本仅新增 `ds_relation_checkpoint`，原 32 张元数据表逐表内容一致，SQLite quick_check 为 ok。

发布前备份一致性 SQLite、runtime、源码及配置；切换后 11 张关键业务表保持一致。源码包 255 个文件，SHA-256 为 `176f61646f21078434286eea6dd381e532b0fd97dcfb1a695f49c2ab3f14a93b`。线上 253 个文件精确匹配，另外两项仍为 `.env.example` 排除和 Docker 对 `.dockerignore` 的自动追加。API ready、401 鉴权及 Nginx 资源哈希核验通过；静态入口 `platform-app-zcxR68GV.js` 的 SHA-256 为 `3c5e57457a353baa774c614575e9e726d4fe0510935e7181855a64e459a2e913`。

### 真实 MySQL 技术验收

本轮以隔离内存定义和只读元数据副本执行技术检查，没有保存或发布测试本体，没有输出业务行值：

- `alpha_account_user` 的 `user_id + union_id + product_key` 完整三列唯一键，同表元组自匹配 100 个样本均命中 1 行；这验证实际 SQL 等值行为，不是新增业务关系。
- 仅取 `user_id` 的 100 个样本也未出现重复匹配；样本无重复不证明该列全表唯一，仍保留真实三列唯一约束。
- 依据历史关系 3530、3531，将 `user.user_create` 与 `user.user_modify` 分别连接到两个 `alpha_user` 实例，实际查询返回 5 行，执行 132 ms。错误角色绑定被结果契约拒绝。
- 当前 26 张表的真实完整键及历史已确认关系没有形成合格的桥表结构候选，因此不宣称真实桥表业务查询已经验收。

记录在服务器 `regression/mysql-technical-acceptance.json`。上述检查不替代独立 Gold SQL 或业务结果核验。

### 完整隔离回归与续跑审计

同一私有工作库经过三轮有界执行，最终退出码 0、状态 `completed`：

| 指标 | 旧回归（部分完成） | 本轮（流程完整） |
| --- | --- | --- |
| 候选数 | 279 | 307 |
| 参考关系进入候选 | 83 / 90 | 83 / 90 |
| 0.55 阈值下历史关系命中 | 65 / 90 | 63 / 90 |
| 标注召回 | 72.22% | 70.00% |
| 未标注正向预测 | 51 | 46 |
| 补采样 | 40，剩余 60 | 119，剩余 0 |
| 提案目录批次 | 10 / 10，但超额被丢弃 | 10 / 10，超额队列为 0 |
| 模型调用 | 27 | 34 |
| 报告 token | 243,959 | 317,295 |

本轮第一轮有 1 个提案批次返回非法 JSON；第二轮只重试该批次并处理剩余候选，第三轮消化最后 39 条补采样。第二轮已有 226 条完成记录保持不变，只新增 2 条候选、补采样 40 条，关系查询增加 125 次、模型调用增加 4 次。第三轮已有 268 条完成记录保持不变，只补采样 39 条，关系查询增加 112 次、模型调用增加 2 次。对完整检查点再次恢复时，307 条记录不变，关系查询与模型调用增量均为 0。

累计实际执行时间 915,270 ms（约 15.3 分钟，不含轮次间等待），包含目录读取/表探针的总查询 1,597 次，其中关系查询 1,216 次。token 为 prompt 270,884 + completion 46,411，34 次调用均有用量返回；没有单价，因此费用仍不推算。后续完整重放仅新增结构核对查询，不混入上述三轮回归数据。`diagnostics.usage` 是全程累计用量，`thisRun.usage` 是本轮增量；嵌套的 `proposal.usage` 来自最近一次提案执行，不是所有提案轮次的合计。

方向准确率 82.54%，基数准确率 66.10%，均只针对命中且有相应标注的关系。完整精确率与完整召回仍为 null。此次不能宣称识别准确性提高：代码补齐了持久化与流程闭环，历史命中反而少 2 条，模型波动和业务证据不足仍需处理，85 分门槛保持原值。

待核验清单共有 99 项：7 条未进入候选、16 条不确定、4 条模型否定、46 条未标注预测，另有 26 条命中关系存在方向/基数差异。离线排查发现 7 条未召回中有 3 条受每列 4 个目标的既有规则上限影响；放宽后的规则集合为 399 条，其他 4 条仍未覆盖。这是召回优化的评测依据，本轮没有据这些不完整正例改写规则或调阈值。

完整报告、逐条理由及匹配证据、两轮增量审计、幂等审计和候选遗漏排查均保存在本轮发布目录的 `regression/`。可审阅文件已导出至项目私有目录 `.data/acceptance/relation-checkpoint-20260907/`（由 Git 忽略）；其中 `RELATION_REVIEW.md` 标明模型方向，避免把反向采样的基数与参考方向混读。

### 生产构建与人工判断保护修复

通过正式本体构建 API 启动任务 `fde9c840-61a7-4f5a-b9bc-f2f63fbc9d47`，按原有选表范围执行，分三轮完成关系分析：26 表、679 字段、272 候选全部判断，97 条补采样均完成复核，10/10 提案批次完成。累计 33 次模型调用，prompt 251,035 + completion 43,918 = 294,953 token，关系采样查询 1,051 次。这是另一次生产执行，与前述隔离回归分别计量。

生产第二轮保留已有 215 条完成记录，只新增 2 条候选、补采样 40 条，增加 120 次关系查询和 5 次模型调用。第三轮保留已有 255 条完成记录，只处理剩余 17 条补采样，增加 49 次关系查询和 1 次模型调用。详细审计在检查点发布目录的 `production/resume-audit-round2.json`、`resume-audit-round3.json`。

首次完成生产分析时，构建有 112 条待确认关系。与构建前快照逐字段比较发现：111 条历史确认记录（按完整等式及其反向去重为 90 条参考关系）的 ID、状态和完整等式均保留，但其中 74 条被模型更新了理由、置信度或证据，28 条还被改动了基数；8 条新待确认项与已有确认关系的反向完整等式相同。

最小失败测试复现后，已从持久化层修复：模型写入按完整等式组查找同向/反向的人工决策，完整保留已接受、已确认、已否定的定义及方向性证据。刷新时停用与这些定义重复的待确认行，关闭失效问题；不删除历史记录。完整联合关系不与单个成员等同，数据库真实外键仍优先；已移除的外键不能被当成人工确认自动沿用。

修复发布目录为 `/data/ontoquery-releases/relation-manual-protection-20260907T1446Z`，镜像 `sha256:d70d9c512c02ab644abc78508d3d5d67aa150cb121a9c4fab52235bd8b07da20`。`confirmed-repair-plan.json` 保存逐条原值与预期当前值，`platform-before-confirmed-repair.sqlite` 保存修复前快照。事务内逐行确认没有并发修改后，只恢复本次模型覆盖的字段；最终全部 111 条历史确认记录与 `platform-before-production-build.sqlite` 逐字段相同。新模型判断完整保留在关系检查点中，发布 schema 不变。恢复结果在 `confirmed-repair-result.json`。

### 探查范围修复与最终上线核验

第一次通过普通探查 API 重放时，输入指纹在采样和模型调用前拒绝恢复。只读诊断表明：实体表没有结构变化，固定构建范围仍为 26 表/679 字段且与原指纹相符；普通入口额外纳入了视图的 26 个字段，变成 26 表/705 字段。根因是 `INFORMATION_SCHEMA.COLUMNS` 包含视图，而探查的字段过滤仅检查排除清单，未要求字段归属于实际纳入的实体表。

范围过滤现统一以纳入的实体表集合约束字段及关系端点。新增场景复现“固定构建范围 → 重启服务 → 普通 API 继续”，修复后正常恢复；真实字段变化仍拒绝。没有改写旧检查点的指纹或放宽输入校验。

最终发布目录 `/data/ontoquery-releases/relation-scope-20260907T1457Z`，镜像 `sha256:2c5ad5e66a0b1032097942ce479adb182fd5e6690fa07f22aab2dcf06bc047fb`；回退标签 `ontology-query-platform-ontoquery:relation-scope-20260907T1457Z-backup` 指向前一人工保护修复镜像。源码包 256 文件，SHA-256 `76d7085219fb5686f0648f14f2aac1a1dc5090c8265a8f78519ed969d88131fb`。

最终镜像在无外网、无生产数据挂载条件下完整执行 `npm run check`：TypeScript 0 错误、ESLint 和构建通过，663/663 测试及 1/1 页面渲染通过。副本初始化前后 33 张元数据表逐表一致。每次切换均先检查没有活动任务，再备份一致性元数据、数据与知识目录、源码与配置；失败自动回退。历史修复已经完成后的最终切换备份保留在该发布目录，不应以修复前的旧快照覆盖已经修复的数据。

最终通过正式 API 重放任务 `0df43e33-c758-494b-b2c1-f7bfe669cb05`：272 条候选、证据、判断和复核记录不变，新增模型调用 0、新增关系采样 0。8 条重复候选停用，111 条历史确认记录逐字段未变。随后不提交业务答案，正式续跑原构建任务以刷新问题清单：当前为 `needs_input`，有 104 条关系待确认，其中 62 条本次新增、42 条既有候选，重复项为 0；尚未开始生成新对象/Link，没有新草稿，也没有发布新版本。

线上验收同时确认：已发布 id 12 / v11（26 对象、36 业务关系）的 schema 及设置不变，列画像关闭、自动确认门槛 85；SQLite quick_check 为 ok，活动任务 0，遗留关系采样查询 0。服务器构建源码 256 文件与清单一致，镜像内 254 文件精确匹配，另外两项仍是 `.env.example` 与 `.dockerignore` 的既定差异。API ready 返回 200、未认证读取返回 401、容器 healthy；从 Nginx HTML 中取得真实 `/_next/static/chunks/platform-app-zcxR68GV.js` 地址后，资源哈希与先前验证值一致。

最终核验、重放和问题刷新结果分别在最终发布目录的 `final-verification.json`、`production-replay-result.json`、`production-question-refresh-result.json`；查询清理检查在 `sampling-cleanup-verification.json`。实际业务审阅清单及 JSON 已下载至项目私有目录 `.data/acceptance/relation-production-20260907/`，入口 `PRODUCTION_RELATION_REVIEW.md`。它记录实际生产待确认关系，不能与隔离回归的 99 项差异清单混用。

后续仍需业务确认这 104 条候选，再继续对象/Link 生成、覆盖/冲突处理和发布验收；还缺独立正反例、标准 SQL 与业务结果，不能计算完整精确率/召回率或据此校准 85 分门槛。桥表能力已有代码及合成验收，但当前数据源没有合格的真实桥表样例。本轮没有自动确认这些关系，也未创建 Git commit。

### 2026-09-08 后续关系核实

上述 104 条是上一轮结束时状态。用户随后选择先核实关系，本轮依照源码、约束和只读样本确认 2 条、否定 5 条，通过正式 API 续跑后剩余 **97 条待确认**。历史 111 条确认记录及其全部字段不变；除 7 个状态外其他关系字段不变，7 个相应问题关闭，其他问题/任务及 30 张受保护元数据表保持不变。原构建任务继续停在 `needs_input`，v11 沿用，无新草稿和模型调用。

本轮没有发布新镜像。容器内持久审计目录 `/var/lib/ontoquery/data/acceptance/relation-verification-20260908/` 保存操作前一致性备份 `platform-before-decisions.sqlite`、确切决定及证据、操作前指纹和 `apply-result.json`。备份留在服务器；本地私有目录 `.data/acceptance/relation-verification-20260908/` 保存有限统计与源码审计。最终 API ready 200、SQLite quick_check 为 ok、活动任务 0。详见[关系核实记录](RELATION_VERIFICATION_20260908.md)。

### 2026-09-08 第二轮核实与双端采样发布

第二轮新增确认 10 条身份关系，原 113 条确认记录逐字段保留；13 条待确认关系的 `1:N` 被实际匹配源端重复反例推翻，仅改为允许多对多的 `N:N`。处理后 confirmed 123、denied 5、rejected 84、review 87，原任务仍为 `needs_input`，无新草稿。关系决策的一致性备份与逐行处理审计在容器持久目录 `/var/lib/ontoquery/data/acceptance/relation-verification-20260908-round2/`；这份备份早于本轮决定，不应与之后的部署备份混用。

应用修复在公共采样中同时计算完整元组的源、目标行数；归一化按两端重复方向修正基数，同一轮补采样失败仍保留此前成功证据。提示词 v4 明确双端指标含义与源端完整唯一性。旧检查点继续保存原始证据，不补造新字段；对旧版本直接恢复关系识别将要求重新探查，当前已完成关系分析的本体任务仍可处理业务答案。本轮没有重跑模型。

发布目录 `/data/ontoquery-releases/relation-source-cardinality-20260908-r2`；镜像 `sha256:c668dba7ab1c15a389cf066805e7bd3b2497e393fdee34d70ca7d65409ac5b4d`，标签 `ontology-query-platform-ontoquery:relation-source-cardinality-20260908-r2`。回退标签为该标签加 `-backup`，指向上一版范围修复镜像 `sha256:2c5ad5e66a0b1032097942ce479adb182fd5e6690fa07f22aab2dcf06bc047fb`。

源码包为 258 个文件，SHA-256 `e63a02d3e451b9eefbc0857196392bd13d5496727fec770fc42029079b1cccf3`。构建复用上一镜像中已锁定的依赖，替换源码后在无外网、无生产数据挂载条件下执行完整 `npm run check`，**670 / 670 测试、TypeScript、ESLint、构建及 1 / 1 页面渲染通过**；本地完整检查同样通过。隔离快照初始化前后 33 张既有元数据表内容完全一致。

首次切换的“所有元数据字段必须完全一致”校验触发自动回退。逐项诊断确认，`seedDemo` 每次启动都会刷新演示来源的 6 个 `ds_table.last_probe_at`、3 个 `ds_relation.evaluated_at`、4 个 `ds_knowledge_page.updated_at`；其他字段及全部非演示记录均未变化。回退保留运行时数据，未用旧库覆盖本轮已完成的关系核实。首次备份、切换脚本、失败结果与诊断保存在 `attempt-1/`。

校验规则随后仅允许这三个时间字段在原有演示来源的原有行上变化，仍拒绝真实来源时间戳变动、演示内容改动、插入/删除、来源标记或结构变化。六类禁止变化的校验样例均被拒绝。重新切换前再次保存一致性 SQLite、数据/知识目录、源码及配置；新镜像启动后验证 33 张表与结构，其中 30 张表完全一致，另 3 张表只有上述 13 个演示时间戳变化，所有非演示记录逐字段一致。

最终核验时间为 2026-09-08 10:04（北京时间）：服务器 258 个源码文件与构建时清单匹配，镜像内 256 个文件精确匹配，另两项是既定的 `.env.example` 排除与 `.dockerignore` 差异。发布完成后追加的本地文档是审计补记，未计入这份构建快照；运行时代码与发布清单一致。容器 healthy，API ready 200，未认证接口 401，SQLite quick_check 为 ok，活动任务 0。已发布 id 12 / v11、配置、检查点与本轮关系处理结果保留；模型检查点累计仍为 33 次调用、294,953 token，本轮增量 0。

上线后使用新镜像执行三条真实 MySQL 限时采样：6826 单列正向、7111 联合列正向、6826 反向，各取 100 个源元组；均获得重复反例并正确将测试输入基数归一化为 `N:N`。采样不保存判断、不改生产元数据、不输出业务行值；随后进程检查遗留采样查询为 0。技术采样结果见 `live-sampling-results.json`，构建、部署与最终核验分别见 `preflight-result.json`、`deployment-result.json`、`final-verification.json`。

备份继续仅存于服务器私有目录。尚需核实剩余 87 条关系，账户口径涉及 42 条且仍待明确；新本体对象/Link、草稿和发布验收尚未执行。没有新增模型质量指标、降低 85 分门槛或创建 Git commit。
